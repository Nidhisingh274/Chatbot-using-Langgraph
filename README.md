# SuperBot – LangGraph + Groq LLM Minimal Agent

SuperBot is a minimal agent built using **LangGraph**, **LangChain**, and **Groq’s LLaMA 3.1 model**.  
This project demonstrates how to build a simple state-based conversational agent with:

- Message state management using LangGraph
- A single-node agent (`SuperBot`)
- Streaming responses (values + updates)
- Integration with Groq's ultra-fast LLM API
- Clean, easy-to-understand minimal code
  
---

## 🚀 Features

- ✔️ **LangGraph StateGraph** implementation  
- ✔️ **SuperBot node** powered by Groq LLaMA 3.1 
- ✔️ **Streaming outputs** (`values` and `updates`)  
- ✔️ **Clean minimal example** with no extra complexity  
- ✔️ Works as a starter template for larger agent systems  

---

